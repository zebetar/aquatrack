

// This file is intentionally left blank as the application is now using a mock data store.
// To switch to a live Firebase backend, this file would need to be populated with
// your Firebase project configuration and the initialization code for the Firebase SDK.

export const db = {};
export const firebaseAuth = {};
